import { InputTypingDirective } from './input-typing.directive';

describe('InputTypingDirective', () => {
  it('should create an instance', () => {
    const directive = InputTypingDirective;
    expect(directive).toBeTruthy();
  });
});
